# E2E Test Suite: Preset Loader
# Verifies markdown-driven emitter path with template preset chain loading.

project:
  code: TEST_PRESET
  name: Preset Loader Tests

output_format: json
docx:
  preset: test_preset_default

# Test case mapping:
# - vc_019_01_preset_load_chain.md -> VC-019 (Preset extends-chain merge)
# - vc_029_06_reference_cache_chain.md -> VC-029 (Whole-chain reference cache invalidation)
