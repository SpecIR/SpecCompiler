# Nested Section

This file lives two directories below the root document. Its figure path is
relative to THIS file, not the root document or the working directory.

```fig:nested-figure{caption="Nested Figure" source="Test"}
img/nested.png
```
