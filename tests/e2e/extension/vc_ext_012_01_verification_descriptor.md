# spec: Verification Descriptor @SPEC-EXT-012

## section: verification registry @VC-EXT-012

Exercises step 8 / HLR-EXT-012: the host owns the ordered policy_key
verification registry. Registering verification descriptors preserves order,
overrides in place by policy_key, removes on disabled, carries the message
hook, and creates the SQL views.
