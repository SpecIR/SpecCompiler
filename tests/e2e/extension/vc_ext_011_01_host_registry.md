# spec: Host Registry Contract @SPEC-EXT-011

## section: descriptor registration @VC-EXT-011

Exercises the host registry (contract.registry): descriptor validation,
dual-emit of type rows, the eager (kind,id)->hooks index, extends-chain
hook inheritance, and loud errors on malformed descriptors.
