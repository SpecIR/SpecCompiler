# spec: Host Float-Hook Index Contract @SPEC-EXT-011C

## section: host registry contract @VC-EXT-011

HOST-REGISTRY CONTRACT TEST (VC-EXT-011 family) -- this Markdown body is a
PLACEHOLDER and deliberately uses no float syntax. The oracle exercises the
host's float hook INDEX directly with a mock data manager and does NOT assert
on the rendered document.

End-to-end float RENDERING (real `csv:` / `table:` / `figure:` syntax ->
checked output) is covered separately by the floats suite (vc_014_*, vc_024_*,
vc_032_*), which exercises the same host-driven dispatch through the full
pipeline.
