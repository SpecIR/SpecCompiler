# Reference DOCX Table Header Test @SPEC-DOCX-REF-TABLE

## Table header alignment

| Short | Multi-line heading probe |
|:------|:-------------------------|
| A     | B                        |

The generated `reference.docx` must top-align first-row table cells without
changing vertical alignment outside the table style's `firstRow` block.
