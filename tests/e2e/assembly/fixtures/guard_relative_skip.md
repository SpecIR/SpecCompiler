# Guard Relative Skip @SPEC-GUARD-REL-SKIP

## Chapter

The included fragment has a root at level 2 and then skips its own level 3.
Normalization must preserve that malformed relative gap.

```include
inc_relative_skip.md
```
