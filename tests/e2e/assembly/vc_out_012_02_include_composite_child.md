# Composite Child Levels @SDD-CC

## CSU: Timer @CSU-TIMER

> file_path: src/timer.c

Component description before the include. The included standalone file nests
under this level-2 object, so its `#` root lands at level 3 and must render as
a child of the component, not as its sibling.

```include
includes/composite/symbols.md
```
